# 手机测试素材

运行 `python scripts/sample-images.py` 可重建素材。这些 127 × 83 的无损小图依据网站对照向量生成，不是网站 JPEG 下载文件，也不是真机导出证明。

- `original.png`：合成风景原图。
- `mixed-once.png`：应点击一次“解混淆”恢复。
- `mixed-twice.png`：应点击两次“解混淆”恢复。

把文件原样传到手机，在 App 中选择图片。请勿先通过聊天软件压缩、截图、裁剪或缩放。也可自行导入照片，混淆后保存为 PNG，再重新导入解混淆。
